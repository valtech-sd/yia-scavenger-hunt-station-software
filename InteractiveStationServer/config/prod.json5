{
  // This is the development (NODE_ENV=dev) config
  // Only items to override from defaults for an ENV need to be added here.
  questConfigFilePath: './config/quest-config-prod.json5',
  // Set this box to a specific BOX ID
  BOX_ID: '10', // Will be overriden by the env var BOX_ID
  // Set this server to a specific SEQUENCE ID
  SEQUENCE_ID: '3',
  // For now, we disable lights in PROD
  lightsEnabled: false,
}
